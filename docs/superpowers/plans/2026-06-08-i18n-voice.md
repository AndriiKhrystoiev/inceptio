# VOICE phase — Localize server-composed astrology copy — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** Compose the astrology VOICE copy in the request locale (de/fr/es-419/pt-BR), thread `X-Locale` through composition + both cache keys, and fill the client `voice.*` files — server-only follow-up to the CHROME phase.

**Architecture:** Leaf `Record<Locale,string>` (a `Localized<string>`) resolved once at each terminal composer function via a tolerant `localize(value, locale)` helper that accepts a plain string (= English-everywhere) OR a Record. This decouples the **locale-threading spine** (Task 0) from the per-dictionary translation (parallel) — the spine wraps every leaf-read in `localize()`, so dictionaries migrate string→Record independently with no coordination.

**Tech Stack:** Cloudflare Worker (Vitest, TS strict), i18next on mobile (already wired). Spec: `docs/superpowers/specs/2026-06-08-i18n-voice-design.md`. Archaeology: the Phase-1A composer audit (counts/call-path/hazards).

---

## Compound V Partition Map

**Task 0 (spine) is SEQUENTIAL + RESERVED owner diff-review** (cross-locale-poisoning boundary). It lands green with dictionaries still English (leaves are plain strings → `localize()` returns en). Then the parallel batch fills locales.

| Task | Owns | Shared? | Phase |
|---|---|---|---|
| **0 Spine** | `routes/search.ts`, `routes/daily-note.ts`, `cache.ts`, `daily-note-cache.ts`, `translations/translate.ts`, `headlines/synthesizer.ts`, `daily-notes/picker.ts`, `daily-notes/composer.ts`, `dictionary/severity-hints.ts` (signature only), `types.ts` (`Locale`/`Localized`/`localize`/`LIBRARY_VERSION`), `index.ts` (`TRANSLATIONS_VERSION`), `lib/locale.ts` (`resolveLocale`) | **ROOT** — sequential, reserved review | seq |
| **G Glossary** | `docs/superpowers/glossary/i18n-termbase.md` (enrich per-locale traditional terms) | feeds translation | seq-ish (before D) |
| **D-factors** | `dictionary/factors.ts` | — | D |
| **D-dailynotes** | `dictionary/daily-notes.ts` | — | D |
| **D-variants** | `dictionary/daily-note-variants.ts` | — | D |
| **D-overrides** | `activity-overrides/{wedding,contracts,business-launch,travel}.ts` | — | D |
| **D-severity** | `dictionary/severity-hints.ts` (data only — Task 0 already touched its signature) | seq after Task 0 | D |
| **D-excluded** | `dictionary/excluded-reasons.ts` | — | D |
| **D-headlines** | `dictionary/headlines.ts` (HEADLINES + NO_VIABLE_HEADLINES data) | card-bound budget | D |
| **D-fallbacks** | `dictionary/daily-note-fallbacks.ts` | — | D |
| **C-voice** | `apps/mobile/src/locales/{de,fr,es-419,pt-BR}/voice/*.json` (20 files) | — | D |
| **T Guard/tests** | coverage-guard flip, per-locale char-budget split, deferred named set | after D | T |

**Disjointness notes:** Task 0 owns ALL composer-logic files (the leaf-READ sites + the `GENERIC_HEADLINE_STEMS` re-architecture in synthesizer + `contextualTag` + the 3 enum-fallback strings, all in `translate.ts`/`synthesizer.ts`). The D-tasks own only their DICTIONARY DATA files (leaf-DATA), disjoint from each other and from Task 0. `severity-hints.ts` is the one file touched by both Task 0 (its `getSeverityHint` signature) and D-severity (its data) — **D-severity runs after Task 0 commits** to avoid the overlap. Everything else is file-disjoint.

**Out-of-scope guard:** do NOT translate `status-lines.ts` / `empty-state.ts` (deferred named set, §7); do NOT merge server↔client blocker copy; do NOT change response/output SHAPE.

---

## Conventions
- **Locale = `'en'|'de'|'fr'|'es-419'|'pt-BR'`.** `localize(v, locale)` = `typeof v === 'string' ? v : (v[locale] ?? v.en)`.
- Worker tests: `cd workers/api-proxy && npx vitest run`. Mobile: `cd apps/mobile && npx vitest run`.
- **Glossary-guided:** every dictionary translation consults `docs/superpowers/glossary/i18n-termbase.md` for the locale's traditional astrology renderings (pt-BR *Lua Fora de Curso*, *Combustão*, etc.). Drafts pending best-effort community review; en authoritative.
- Commit per task; typecheck + tests green before commit; lock-retry on parallel commits.

---

## Task 0 — Locale-threading spine (SEQUENTIAL · RESERVED owner diff-review)

**Files:** as in the map. **Test:** `workers/api-proxy/src/__tests__/locale-voice-spine.test.ts` + updates to `cache`/`daily-note` tests.

- [ ] **Step 1 — Types.** In `types.ts`: add `export type Locale = 'en'|'de'|'fr'|'es-419'|'pt-BR'`; `export type Localized<T=string> = T | Record<Locale, T>`; `export function localize<T>(v: Localized<T>, locale: Locale): T { return (v && typeof v === 'object' && 'en' in v) ? ((v as Record<Locale,T>)[locale] ?? (v as Record<Locale,T>).en) : (v as T); }`. Bump `LIBRARY_VERSION`.
- [ ] **Step 2 — `resolveLocale`.** In `lib/locale.ts` add `export function resolveLocale(raw: string|null): Locale { return (raw && (['de','fr','es-419','pt-BR'] as const).includes(raw as any)) ? raw as Locale : 'en'; }` (absent/unknown → 'en'). Unit-test it.
- [ ] **Step 3 — Thread `locale` (failing test first).** Write `locale-voice-spine.test.ts` asserting: (a) `translate(envelope, activity, 'de')` resolves a factor leaf via `localize`; (b) the strong-form cache test — `computeCacheKey(req,'de') !== computeCacheKey(req,'en')` and daily-note `keyOf({...,locale:'de'}) !== keyOf({...,locale:'en'})`. Run → fails (signatures lack `locale`).
- [ ] **Step 4 — Implement threading.** Add a non-optional `locale: Locale` param to every signature on the path (see spec §3): routes capture `resolveLocale(req.headers.get('X-Locale'))` once and thread it; `translate`/`translateFactor`/`translateExcludedReason`/`translateExcluded`/`pickTagline`/`contextualTag`/`synthesizeHeadline`/`synthesizeDailyNote`/picker helpers/`composeDisplayable`/`getSeverityHint` all gain `locale`. Wrap EVERY leaf-read in `localize(<leaf>, locale)`.
- [ ] **Step 5 — Cache keys (same change-set).** `computeCacheKey(req, locale)` → locale in prefix `search:v1:t${TRANSLATIONS_VERSION}:${locale}:`. `keyOf` + `DailyNoteCacheKey` → append `:${locale}`; thread `locale` into the `cacheKey` object in `daily-note.ts`. Bump `TRANSLATIONS_VERSION` (`index.ts`).
- [ ] **Step 6 — Grammar hazards (spine-owned).** Re-architect `GENERIC_HEADLINE_STEMS` to a per-locale template with a `{lead}` slot (NO `lowerFirst`; German nouns stay capitalized) — initially all-en templates; the per-locale fills land in D-headlines. Move `contextualTag` inline literals into a localized constant. Convert the 3 enum-fallback strings (`FALLBACK_REASON_PHRASE`, `FALLBACK_FACTOR_PHRASING`) to `Record<Locale,string>` (en-filled-everywhere for now).
- [ ] **Step 7 — Verify + STOP for owner review.** `npx vitest run && npx tsc --noEmit` green (composition still all-English, but locale-keyed + cache-keyed). Stage, do NOT commit. Hand the diff to the owner (reserved review: confirm both cache keys carry locale, strong-form test passes, threading is non-optional, no shape change). Commit only on approval: `feat(worker): VOICE spine — thread X-Locale through composition + both cache keys`.

---

## Dictionary Localization Procedure (every D-task)

- [ ] **Step 1 — Budget test first** (where applicable): write/extend the per-locale char-budget assertion for this file's strings (see Task T for the split: card-bound headlines hard ≤ derived limit; screen-bound ≤48/140/150). Run → fails on missing locales.
- [ ] **Step 2 — Convert leaves to `Record<Locale,string>`**: each English leaf `'x'` → `{ en: 'x', de: <t>, fr: <t>, 'es-419': <t>, 'pt-BR': <t> }`. Translate de/fr/es-419/pt-BR **glossary-guided**, register-correct (de=du, fr=tu, es-419 voseo-neutral, pt-BR você). Keep structural metadata (`pending_astrologer_ruling`, `horizon_class`, etc.) ABOVE the leaf. For `pending_astrologer_ruling` entries: translate provisionally to match the current English; the flag stays so they re-translate independently.
- [ ] **Step 3 — Verify**: `npx vitest run` (budget + the all-5-locales leaf test) + `npx tsc --noEmit` green.
- [ ] **Step 4 — Commit** `feat(worker): localize <file> (N strings × 4 locales)`.

### Per-task inventories (counts + special handling)
- **D-factors** — `factors.ts`, 90 strings (15 IDs × pass/partial/fail × short/full). Largest.
- **D-dailynotes** — `daily-notes.ts`, 42 (21 × headline+supporting). Screen-bound budget ≤48/140. 2 entries `pending_astrologer_ruling` (closed-mercury/venus-retrograde) → provisional.
- **D-variants** — `daily-note-variants.ts`, 36 (variant pools).
- **D-overrides** — 4 activity-override files, 36 total (6/12/10/8 phrase_short+full).
- **D-severity** — `severity-hints.ts`, 16 (4 conditions × 4 activities). **Screen-bound ≤150.** 4 `moon_voc_intraday` pending → provisional (runtime-hidden; translate speculatively).
- **D-excluded** — `excluded-reasons.ts`, 11. 3 pending (mercury_combust, mars/jupiter_retrograde) → provisional.
- **D-headlines** — `headlines.ts`, 18 (HEADLINES 10 + NO_VIABLE 4 + stem per-locale fills 4). **CARD-BOUND** — these render on the fixed share image. Hard budget (Step 1): German MUST fit the derived card capacity; irreducible → concise rewrite / hold English (NOT deferred). Fill the per-locale GENERIC_HEADLINE_STEMS templates Task 0 scaffolded.
- **D-fallbacks** — `daily-note-fallbacks.ts`, 6. 1 pending → provisional.
- **C-voice** — fill `{de,fr,es-419,pt-BR}/voice/{reason,moment,card,calendar,moments}.json` (41 keys/locale). `voice/card.json` TIER_PHRASES are **card-bound** (in the share-image pill) — keep short.

---

## Task T — Guard flip + char-budget split + deferred set (after all D)

**Files:** `workers/api-proxy/src/translations/__tests__/lint-library.test.ts` (split), a new server `voice-coverage.test.ts`, and `apps/mobile/src/i18n/__tests__/coverage.test.ts` (flip).

- [ ] **Step 1 — Server all-locales leaf coverage.** Test asserting every localized leaf `Record` in every dictionary has all 5 locale keys (no missing locale). The deferred named set `VOICE_DEFERRED = ['status-lines','empty-state']` is exempt — assert the set is EXACTLY those two (can't grow), tied to the "translate when saved-search fan-out ships" comment.
- [ ] **Step 2 — lint split.** In `lint.ts`/its test: **language-content checks (forbidden-word/horizon/`.includes()`/3-day rule) run on en ONLY**; **structural + char-budget checks run on ALL 5 locales.** Do not scope the budget gate to en.
- [ ] **Step 3 — Card-bound vs screen-bound budgets.**
  - **Card-bound (hard):** window headlines (`headlines.ts` HEADLINES/stems/NO_VIABLE) ≤ the derived card limit (≈ the longest English at 26px/360px/2-lines — measure and pin a char ceiling), enforced for all 5 locales. A German overflow here FAILS the build (card correctness, not a warning).
  - **Screen-bound (all-locales, soft):** daily-note headline ≤48 / supporting ≤140 / severity ≤150 — assert for all 5 locales; a failure is the German-layout-pass signal, but keep it a real assertion so overflow is detected. (If the owner wants screen overflow non-blocking, mark those `it`s with a documented soft-fail/report — default: keep as assertions and let the layout pass widen the client if needed.)
- [ ] **Step 4 — Client coverage flip.** In mobile `coverage.test.ts`: `voice.*` keys must now exist in ALL 5 locales (remove the en-only inverse); `onboarding:subhead` stays the length-1 CHROME exception (unchanged).
- [ ] **Step 5 — Verify + commit** both packages green; `test(i18n): VOICE coverage flip + card/screen budget split + deferred named set`.

---

## Self-Review
- Spec coverage: §2 mechanism → Task 0 `localize`; §3 spine → Task 0; §4 budgets → Task T Step 3 + D-headlines/D-severity; §5 hazards → Task 0 Step 6; §6 ruling-pending → D-task Step 2; §7 deferred → Task T Step 1; §8 fallbacks → Task 0 Step 6; §9 guard/glossary → Task T + Task G; §10 partition → map. Covered.
- Type consistency: `Locale`/`Localized`/`localize`/`resolveLocale` used uniformly.
- Cross-locale poisoning (the load-bearing bit): both cache keys + strong-form test in Task 0, reserved review.
