// postAlertAck removed from api.ts in Task 3.3 (remove-cloudflare migration).
// This test file is deleted in Task 3.5; entire suite skipped here so the
// full test run stays green while the deletion is pending.
import { describe, it } from 'vitest';

describe.skip('postAlertAck — removed in 3.5', () => {
  it('placeholder', () => {});
});
